# Quixote

A fast, keyboard-driven macOS tool for enriching structured data files with LLMs.

## Get Started

```sh
make setup      # install xcodegen (once)
make generate   # generate Quixote.xcodeproj
make dev        # build and launch the app
```
